/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = 

module.exports = {

  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com'],
    nextConfig,
  },
};
